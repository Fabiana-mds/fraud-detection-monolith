services:
  db:
    image: postgres:16
    container_name: fraud_db_container
    environment:
      POSTGRES_DB: fraudchecker_db
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_password
    ports:
      - "5432:5432"